import styled from 'styled-components'

const MainContainer = styled.div`
    width: 75vw;
    height: 100vh;
    display: flex;
    flex-flow: row wrap;
    padding: 50px;
`

export default MainContainer
